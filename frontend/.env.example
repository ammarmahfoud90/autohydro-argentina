# Local dev — Vite proxy handles /api/* so VITE_API_URL can be empty
VITE_API_URL=

# Production (Vercel) — set to your Railway backend URL
# VITE_API_URL=https://your-backend.up.railway.app

VITE_DEFAULT_LANGUAGE=es
